const { completeProfile } = require("./completeProfile");
const { getProfile } = require("./getProfile");
const { getAll } = require("./getAll");
const { create } = require("./create");
const { remove } = require("./deleteDoctor");

module.exports = {
  completeProfile,
  getProfile,
  getAll,
  create,
  remove,
};
