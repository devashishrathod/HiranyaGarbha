const campaigns = require("./campaigns");
const feed = require("./feed");
const devices = require("./devices");
const templates = require("./templates");

module.exports = { campaigns, feed, devices, templates };
