# Claude Code personal settings. The local file records ad-hoc command
# allowlists and can capture bearer tokens from commands that were run, so it
# stays out of the repo; .claude/settings.json is the shared one.
.claude/settings.local.json
